stages:
- build
- test
- deploy

build-backend:
  stage: build
  script:
  - echo "Compiling the code..."
  rules:
  - changes:
    - BackEnd/**/*

test-backend:
  stage: test
  script:
  - echo "Running unit tests..."
  rules:
  - changes:
    - BackEnd/**/*

deploy-backend:
  stage: deploy
  script:
  - echo "Deploying application. backend.."
  rules:
  - changes:
    - BackEnd/**/*